// Copy this file to secrets.h and fill in your own values.
// secrets.h is gitignored — never commit it.
#ifndef SECRETS_H
#define SECRETS_H

#include <Arduino.h>

// Wi-Fi credentials
const char *ssid = "YourWifiName";
const char *password = "YourWifiPassword";

// Telegram bot token from @BotFather
#define BOTtoken "1234567890:AAAA-your-bot-token-here"

// Your personal Telegram chat id (get it from @userinfobot).
// Only this chat is allowed to control the relays.
#define CHAT_ID "123456789"

// Devices whose presence means "the owner is home".
// {"Label", "MAC as 12 hex digits"} — colons/dashes are also accepted.
// NOTE: modern phones randomize their MAC per network; use the MAC your
// phone presents on YOUR Wi-Fi (or disable randomization for it).
String KnownMac[][2] = {
    {"My phone", "A1B2C3D4E5F6"},
    {"My laptop", "0A:1B:2C:3D:4E:5F"},
};

#endif
